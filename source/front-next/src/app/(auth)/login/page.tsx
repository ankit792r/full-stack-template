import LoginForm from "@/components/templates/login-form";

// this will only render the login component
// it contains inputs, buttons, and other UI elements
// it may be a client side component
export default function Page() {
    return <LoginForm />
}